form(
  action="/users/messages",
  method="post",
  onSubmit="event.preventDefault();partial.form(this, [['ul', 'appendChild'], ['form', 'replace']]);"
)
  input(name="message") 
  input(type="submit")
